#include <unistd.h>
#include "main.h"
#include <stdio.h>

/**
* mul -  outputs the multiplication
* @a: parameter a is the number
* @b: parameter b is the number
*
* Return: Always 0 (Success)
*
*/

int mul(int a, int b)
{
	return (a * b);
}

